const gulp = require('gulp');

gulp.task('sass', () => {
});

gulp.task('sass-clean', () => {
});

gulp.task('sass-watch', () => {
});

module.exports = {
	build: 'sass',
	clean: 'sass-clean',
	watch: 'sass-watch'
};
