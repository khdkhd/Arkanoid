const gulp = require('gulp');

gulp.task('sass', () => {
});

gulp.task('sass-clean', () => {
});

gulp.task('sass-watch', () => {
});

module.exports = {
	build: 'bundle',
	clean: 'bundle-clean',
	watch: 'bundle-watch'
};
