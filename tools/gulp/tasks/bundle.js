const gulp = require('gulp');

gulp.task('bundle', () => {
});

gulp.task('bundle-clean', () => {
});

gulp.task('bundle-watch', () => {
});

module.exports = {
	build: 'bundle',
	clean: 'bundle-clean',
	watch: 'bundle-watch'
};
